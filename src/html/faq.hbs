<!DOCTYPE html>
<html>
<head>
	<meta charset="UTF-8">
	<title>ZBib FAQ</title>
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<link rel="stylesheet" type="text/css" href="/static/bib.css">
	<link rel="shortcut icon" href="/static/favicon.ico" type="image/x-icon">
	<script>
		window.onload = function () {
			var headers = document.querySelectorAll('h2, h3');
			for (let header of headers) {
				let a = document.createElement('a');
				a.href = '#' + header.id;
				a.textContent = '#';
				header.appendChild(a);
			}

			var cssFile = window.navigator.platform.includes('Win') ? 'fonts-win.css' : 'fonts-mac.css';
			var element = document.createElement('link');
			element.setAttribute('rel', 'stylesheet');
			element.setAttribute('type', 'text/css');
			element.setAttribute('href', `/static/${cssFile}`);
			document.getElementsByTagName('head')[0].appendChild(element);
		};
	</script>
</head>
<body>
	<nav><a href="/">Back to ZBib</a></nav>
	<main id="faq">{{{faq}}}</main>
</body>
</html>
